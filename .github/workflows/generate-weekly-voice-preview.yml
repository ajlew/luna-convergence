name: Retired - generate-weekly-voice-preview.yml
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Generate Luna plain-text readings. This legacy workflow no longer generates or publishes readings."
